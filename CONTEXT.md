# CONTEXT

领域词汇表。仅收录产品域术语；实现细节与通用编程概念不在此列。

**Realtime Notification（实时通知）**: 用户登录后通过长连接接收的轻量事件提示，告知某资源可能已变化；客户端仍须经 REST 拉取最新数据。  
_Avoid_: 全量快照推送、把 WebSocket 当作命令通道、把推送帧当作真相源。

**Job Progress（任务进度）**: 资产核对等异步任务在执行过程中的状态与阶段变化（如排队、校验、运行、收尾、终态）。  
_Avoid_: 伪造百分比进度、与上传进度（tus）混为一谈。

**Tools Meta（工具元数据）**: 管理员对工具目录项的运行时覆盖（启用/禁用、排序、自定义名称与描述），影响主控台可见工具列表。  
_Avoid_: 与前端静态 `toolsConfig` 定义本身混淆；静态配置仍是代码源，元数据是覆盖层。
